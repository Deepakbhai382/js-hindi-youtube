function calculateTotal(price, quantity) {
    const total = price * quantity;
    
    if (total > 1000) {
        console.log('Large order');
    }
    
    return total(45, 60);
}

console.log( total);
